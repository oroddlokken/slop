# Claude Code Hooks

PreToolUse hooks for Claude Code's Bash tool. Symlinked to `~/.claude/hooks/`.

Canonical copies live in [slop/claude/hooks](https://github.com/oroddlokken/slop/tree/main/claude/hooks).
